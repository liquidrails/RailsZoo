<h1>Listing postings</h1>
<h2>Query = <%= h @query %></h2>

<% for subcat in @subcat_array %>
  <%= subcat.id %><br>
<% end%>

<% if nil != @chunks %>
<h2>C H U N K S</h2>
	<% for chunk in @chunks %>
	  <%= chunk %><br>
	<% end%>
<% end %>

<h2> Query String </h2>
  <%= @query_string %>


<table>
  <tr>
    <th>Headline</th>
    <th>Details</th>
    <th>Organizer</th>
    <th>Contact</th>
    <th>Link</th>
    <th>Email</th>
    <th>Departure</th>
    <th>Return</th>
    <th>Duration</th>
    <th>Subcategory</th>
    <th>Phone</th>
    <th>Key</th>
    <th>Validated</th>
  </tr>

<% for posting in @postings %>
  <tr>
    <td><%= posting.headline %></td>
    <td><%= posting.details %></td>
    <td><%= posting.organizer %></td>
    <td><%= posting.contact %></td>
    <td><%= posting.link %></td>
    <td><%= posting.email %></td>
    <td><%= posting.departure %></td>
    <td><%= posting.return %></td>
    <td><%= posting.duration %></td>
    <td><%= posting.subcategory_id %></td>
    <td><%= posting.phone %></td>
    <td><%= posting.key %></td>
    <td><%= posting.validated %></td>
    <td><%= link_to 'Show', posting %></td>
    <td><%= link_to 'Edit', edit_posting_path(posting) %></td>
    <td><%= link_to 'Destroy', posting, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
Pagination... da da....<%= will_paginate(@postings) %>
<br />

<%= link_to 'New posting', new_posting_path %>
