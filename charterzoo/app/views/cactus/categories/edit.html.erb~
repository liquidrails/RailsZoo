<br>
<h2>Edit category</h2>

<%= error_messages_for :category %>

<% form_for([:cactus, @category]) do |f| %>

<p>
  <b>ID:</b>
  <%=h @category.id.to_s %>
</p>

  <p>
    <b>Name</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', cactus_category_path(@category) %> |
<%= link_to 'Back', url_for(:controller =>"cactus/admin", :action => "index") %>
<br><br>
<%= link_to 'Delete', [:cactus, @category], :confirm => 'Are you sure?', :method => :delete %>
