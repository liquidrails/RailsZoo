<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>Charterzoo</title>
  <%= stylesheet_link_tag 'cat' %>
  <LINK REL="icon" HREF="images/favicon.ico" TYPE="image/ico">
  <LINK REL="SHORTCUT ICON" HREF="images/favicon.ico">
</head>

<% @categories = Category.find(:all)%>

<body>
<table width ="<%= TABLE_WIDTH %>" align="center" height="80" border="0" cellpadding="0" cellspacing="0" bgcolor="#ffffff">

<tr bgcolor=#ffffff><td colspan=1  align="left"><span class="slogan"><i>Join a Charter or Post Your Own!</i></span></td>
<td width = "<%= TD_RIGHT %>" align="right" valign="top" />

      <% form_tag :action => 'search', :controller=>'postings' do %>
           <input type="hidden" name="location" value="<%= @location.id %>">
           <span class="menu" ><%= link_to 'Post', new_location_post_path(@location.id) %>  |  Search </span>
           <input id="query" name="query" size="25" type="text" font-size="8pt" class="pulldown" />

	   <select id="category_id" name="category[id]" class="pulldown" >
             <% for category in @categories %>
	       <option value="'c <%= category.id %>'">All <%=category.name %></option>
             <% end %>
           </select>
      <%= submit_tag '>' %>

      <% end %>

   </td>

</tr>


<tr>
<td width="<%= TD_LEFT %>" valign="top" align="left" colspan=2><%=link_to image_tag ("../images/charterzoo_logo_2k.gif", :alt=>'Charterzoo', :border=>0), home_location_path(@location) %> 
</td>

    
  </tr>

<tr><td align="left" colspan=1><span class="titleBold"><%=link_to @location.name, home_location_path(@location) %></span> </td>
<td align="right">&nbsp;</span></td>
</tr>
</table>
<br>
<%=yield  %>

</body>
</html>
